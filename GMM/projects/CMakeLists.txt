IF (${BUILD_GMM_TRAIN})
	MESSAGE (STATUS "MODEL ADAPT")
	add_subdirectory (Model_ADAPT)
ENDIF (${BUILD_GMM_TRAIN})

IF (${BUILD_GMM_LOGLIKE})
	MESSAGE (STATUS "GMM LOG-LIKE")
	add_subdirectory (GetLogLikeGMM)
ENDIF (${BUILD_GMM_LOGLIKE})
